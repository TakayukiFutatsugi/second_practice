<h2>求職者様新規登録</h2>
<div class="ui attached message">
  <div class="header">新規登録誠にありがとうございます</div>
  <p>以下の登録事項をご記入ください</p>
</div>

<form class="ui form attached fluid segment">
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= render "candidates/shared/error_messages", resource: resource %>

    <div class="two fields">
      <div class="field">
        <%= f.label :姓 %>
        <%= f.text_field :familyName %>
      </div>
      <div class="field">
        <%= f.label :名 %>
        <%= f.text_field :lastName %>
      </div>
    </div>
    
    <div class="two fields">
      <div class="field">
        <%= f.label :セイ %>
        <%= f.text_field :familyNameDetail %>
      </div>
      <div class="field">
        <%= f.label :メイ %>
        <%= f.text_field :lastNameDetail %>
      </div>
    </div>
  
    <div class="field">
      <%= f.label :性別 %>
      <%= f.select :gender, Candidate.genders.keys, {} %>
    </div>
  
    <div class="three fields">
      <%= f.label :生年月日 %>
      <%= f.date_select(:birthdate, start_year: 1970, end_year: Date.today.year) %>
    </div>
  
    <div class="field">
      <%= f.label :居住地 %>
      <%= f.select :prefecture, Candidate.prefectures.keys, {} %>
    </div>
  
    <div class="field">
      <%= f.label :メールアドレス %>
      <%= f.email_field :email, autocomplete: "email" %>
    </div>
    
    <div class="field">
      <%= f.label :パスワード %>
      <% if @minimum_password_length %>
        <em>(<%= @minimum_password_length %> 文字以上)</em>
      <% end %>
      <%= f.password_field :password, autocomplete: "new-password" %>
    </div>
    
    <div class="field">
      <%= f.label :パスワード確認 %>
      <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
    </div>
    
    <div class="inline field">
      <div class="ui checkbox">
        <input type="checkbox" id="terms">
        <label for="terms">利用規約に同意する</label>
      </div>
    </div>
    
    <div class="actions">
      <%= f.submit "登録", method: :post %>
    </div>
  <% end %>
</form>

<div class="ui bottom attached warning message">
  アカウントをお持ちの方は <a href="#">こちら</a> からログイン
</div>

<%= render "candidates/shared/links" %>